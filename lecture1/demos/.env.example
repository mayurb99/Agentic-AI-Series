# Copy this file to ".env" in the same folder and fill in your real key.
# NEVER commit the real ".env" file — it is already listed in .gitignore.

# Required for all 3 demos. Get a free key at https://console.groq.com/keys
# This DevOps track uses Groq exclusively for every lecture until told otherwise.
GROQ_API_KEY=your_groq_api_key_here
