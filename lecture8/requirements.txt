# Agentic AI for DevOps Engineers — requirements.txt
# Lecture 8: On-call runbook helper (ingest + RAG tool + Streamlit)
# Install: pip install -r requirements.txt
#
# Same LangChain stack as L4–L7, plus Chroma/embeddings for RAG-as-a-tool,
# and Streamlit for the browser chat UI.

langchain>=1.0
langchain-groq>=0.2.0
langchain-text-splitters>=0.3.0
langchain-chroma>=0.2.0
langchain-community>=0.3.0
langchain-huggingface>=0.1.0
langgraph>=0.2.0
pypdf>=4.0.0
sentence-transformers>=3.0.0
chromadb>=0.5.0
python-dotenv>=1.0.0
pydantic>=2.0.0
streamlit>=1.28.0

# Needed by transformers/sentence-transformers (ZoeDepth optional import path)
torchvision

