# Lecture 2 does NOT need a Groq API key (or any API key).
# All three demos run fully offline with sentence-transformers + chromadb
# once the embedding model is cached from the first demo_1 download.
#
# Groq returns in Lecture 3 (RAG). If you already have a key from Lecture 1,
# you can ignore this file for now — nothing in lecture2 reads GROQ_API_KEY.
#
# (Left as a stub so students who copy Lecture 1's setup habit don't get
# confused looking for a missing .env.example.)
# GROQ_API_KEY=not_needed_for_lecture2
